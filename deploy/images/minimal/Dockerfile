# OpenClaw Agent - Minimal Profile
# Bare minimum for basic tasks - fast startup, small footprint

FROM ghcr.io/openclaw/agent:base

LABEL org.opencontainers.image.title="OpenClaw Agent - Minimal"
LABEL org.opencontainers.image.description="Minimal OpenClaw agent with basic CLI tools"
LABEL platform.openclaw.io/profile="minimal"

# No additional packages - uses base image only

# Profile metadata
ENV OPENCLAW_PROFILE="minimal"
ENV OPENCLAW_PROFILE_VERSION="1.0.0"
